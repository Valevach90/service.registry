spring:
  kafka:
    groupId: my_group
    bootstrapAddress: localhost:9092
    maxRetryAttempts: 3
    consumer:
      auto-offset-reset: earliest
      group-id: my_group
    topicTransferRequest: transfer_to_deposit_request
    topicTransferResponse: to_transfer_response

